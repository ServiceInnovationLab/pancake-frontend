import React, { Fragment } from 'react';

const selectField = ({ input, label, type, className, meta: { touched, error } }) => (
  <Fragment>
    <select></select>
  </Fragment>
);

export default selectField;
